# Балтийский Телекоммуникационный Холдинг

## Тестовое задание для Junior Full-Stack Developer (Laravel + Vue.js)

### 🎯 Цель

Разработать простое веб-приложение — каталог товаров с административной панелью для их управления.

---

## 🧰 Стек

- **Бэкенд:** Laravel 10+ (RESTful API)
- **Фронтенд:** Vue.js 3 (Composition API) + InertiaJS — обязательно
  Можно использовать UI-библиотеку (например, Element Plus, PrimeVue или Tailwind CSS)
- **База данных:** PostgreSQL

---

# Часть 1: Бэкенд (Laravel API)

Разработать RESTful API со следующим функционалом:

---

## 1. Модели и миграции

- **Product (Товар):**
  `id`, `name`, `description`, `price`, `category_id`, `created_at`, `updated_at`

- **Category (Категория):**
  `id`, `name`, `description`, `created_at`, `updated_at`

- Связь:
  Один товар принадлежит одной категории (`belongsTo`).
  Одна категория имеет много товаров (`hasMany`).

---

## 2. API эндпоинты (Resource-контроллеры + Form Request Validation)

### 📦 Товары (Products)

- `GET /api/products` — список всех товаров с пагинацией (10–15 на страницу)
  ✔ включает информацию о категории

- `GET /api/products/{id}` — просмотр одного товара

- `POST /api/products` — создание товара _(требуется аутентификация)_

- `PUT/PATCH /api/products/{id}` — обновление товара _(требуется аутентификация)_

- `DELETE /api/products/{id}` — удаление товара _(требуется аутентификация)_

---

### 📁 Категории (Categories)

- `GET /api/categories` — список всех категорий
  ✔ без пагинации (для выпадающего списка)

---

## 3. Аутентификация (Sanctum)

- Реализовать простую аутентификацию по токену для административных действий
  (создание / редактирование / удаление)

- Достаточно:
  - одного предустановленного пользователя (логин/пароль)
    **или**
  - эндпоинта `POST /api/login`, возвращающего токен

- Эндпоинты на чтение (`GET`) должны быть публичными

---

## 4. Валидация

При создании/обновлении товара проверять:

- `name` — обязательно, строка
- `price` — обязательно, число, больше 0
- `category_id` — должен существовать в таблице категорий

---

# Часть 2: Фронтенд (Vue.js)

---

## 1. Публичная часть (без аутентификации)

- `/` — список товаров с пагинацией
  ✔ отображать: название, категорию, цену, краткое описание

- `/product/{id}` — подробная информация о товаре

- Фильтрация:
  - выпадающий список категорий 
  - при выборе → AJAX-запрос с фильтром

---

## 2. Административная часть (с аутентификацией)

- Логин:
  - страница ввода логина/пароля
  - токен хранится в localStorage

- После входа:
  - меню:
    - "Управление товарами"
    - "Выйти"

- `/admin/products`:
  - список товаров (как публичный)
  - кнопки:
    - "Редактировать"
    - "Удалить"
    - "Добавить товар"

---

## Форма создания / редактирования товара

- `/admin/products/create`
- `/admin/products/{id}/edit`

Поля:

- Название (input)
- Категория (select, загружается из `GET /api/categories`)
- Описание (textarea)
- Цена (input type="number")

✔ Валидация на фронтенде (простая: required, min для цены)

✔ Отправка данных в API

✔ После успеха → редирект на список товаров

---

# Что мы оцениваем (критерии)

---

## 1. Общее

- Работоспособность всего функционала
- Чистота и структура кода
- Именование переменных и функций
- Работа с Git (не один коммит "finish")

---

## 2. Бэкенд (Laravel)

- Использование Eloquent (relations, `with`)
- Разделение логики:
  - контроллеры
  - модели
  - Form Request
- Корректные HTTP-коды
- Resource / Collection для API

---

## 3. Фронтенд (Vue.js)

- Composition API (`ref`, `reactive`, `computed`, `watch`)
- Работа с состоянием (токен, загрузка, ошибки)
- Обработка ошибок API

---

# Бонусные задачи (не обязательно)

---

## 🧩 Общее

---

## 🧠 Бэкенд

- Docker + docker-compose
- Soft Deletes для товаров
- Seeders (категории + товары)
- Базовые тесты (например, создание товара)

---

## 💻 Фронтенд

- Composables (`useProductApi`, `useAuth`)
- Красивый адаптивный UI (UI библиотека)
- Подтверждение удаления (modal)
- Debounce для поиска (если добавишь)

---

# 📦 Деплой

Готовое приложение выложить на GitHub / Bitbucket
